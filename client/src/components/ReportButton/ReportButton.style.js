import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    myButton: {
        marginLeft: "2px",
        position: "absolute",
        textTransform: "none"
    },
    buttonText: {
        fontSize: '12px'
    }
}));